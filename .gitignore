# EPC drawings are NOT published.
#
# publish/ is the GitHub Pages root (CNAME -> tendrara.neo-ware.com), so anything
# committed here is served anonymously on the public internet. The 90 ESA/ITALFLUID
# PDFs were being served that way — they now live in the PRIVATE Supabase Storage
# bucket `plant-docs` and are reached through signed URLs (doc-url Edge Function),
# which require a signed-in session.
#
# Do not remove this rule. If you need a drawing on the site, it belongs in storage
# and gets linked from the E&I Navigator (EI70 / EI80), not committed here.
# NOTA (2026-07-28): esto decia `docs/` — con barra final git excluye el DIRECTORIO
# y ya no mira dentro, asi que los dos `!docs/...` de abajo NUNCA se aplicaron y los
# handouts publicos llevaban meses sin publicarse (git ls-files no devolvia un solo
# PDF). `docs/*` excluye el CONTENIDO, que es lo que permite reincluir excepciones.
docs/*

# The two genuinely public handouts, linked from the training decks:
!docs/LNGSafetyData.pdf
!docs/SAF05-ING-FR01.pdf

# --- LISTA BLANCA DEL MODULO 101 (decision de Mario, 2026-07-28) ---------------
# El deck de formacion tiene que ser INDEPENDIENTE: los alumnos no tienen cuenta
# del E&I Navigator, asi que sus documentos se sirven desde /docs sin login.
# Esto es una EXCEPCION consciente a la regla de arriba, acotada a estos ficheros:
# quedan publicos en tendrara.neo-ware.com. Todo lo demas de docs/ sigue fuera.
# Para revertir: borrar estas lineas y las copias de publish/docs.
!docs/20250310_TAM-158_PFD.pdf
!docs/PRO02-ING-FR02.pdf
!docs/PRO13-ING-PR01.pdf
!docs/PRO13-ING-PR50.pdf
!docs/PRO13-ING-PR51.pdf
!docs/21044-PR-DSH-0E201.pdf
!docs/SAF01-ING-FR01.pdf
!docs/SAF12-ING-FR01.pdf
!docs/21044-AU-HMI-00002.pdf
!docs/21044-PR-MAN-00001.pdf

# Local tooling / secrets — must never reach a public repo
node_modules/
.env
*.local
